# Tooling Baseline (minimal)

- Prefer the repo's existing package manager and scripts.
- Include only essential lint/format/test configs to satisfy the request.
- Avoid global refactors; touch only files required by the change.
