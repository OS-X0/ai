# Structure Guidelines

- Follow existing folder naming and nesting.
- Prefer feature-based groupings over deep technical layers unless the repo already uses layers.
- Keep shared utilities in a clearly named shared/common module.
